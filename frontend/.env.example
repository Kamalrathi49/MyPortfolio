# While developing, point at your local API (no CORS issues):
VITE_API_BASE_URL=http://localhost:8000

# Production API from local Vite — requires api.kamalrathi.dev to allow localhost
# (backend merges localhost dev origins unless CORS_STRICT=true):
# VITE_API_BASE_URL=https://api.kamalrathi.dev
