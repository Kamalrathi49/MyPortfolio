DATABASE_URL=sqlite:///./portfolio.db
# PostgreSQL example (requires: pip install psycopg2-binary):
# DATABASE_URL=postgresql+psycopg2://user:pass@localhost:5432/portfolio
API_KEY=change-me-to-a-long-random-secret

SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=you@example.com
SMTP_PASSWORD=your-smtp-password
SMTP_FROM=you@example.com
CONTACT_NOTIFY_TO=you@example.com

CORS_ORIGINS=https://kamalrathi.dev,https://www.kamalrathi.dev,http://localhost:5173,http://127.0.0.1:5173
# Set to true to allow only CORS_ORIGINS (no auto-added localhost dev origins):
# CORS_STRICT=true
